apiVersion: v1
kind: Service
metadata:
  name: opencode
  namespace: default
  labels:
    app: opencode
spec:
  type: ClusterIP
  selector:
    app: opencode
  ports:
    - name: http
      port: 4096
      targetPort: http

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opencode-data
  namespace: default
  labels:
    app: opencode
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
